language: node_js
node_js:
  - "10.3.0"

before_install:
  - npm i -g npm@6.9.0
  - 

cache:
  directories:
    - node_modules

branches:
  only:
    - development

script:
  - npm install
  - npm build

after_success:
  - firebase deploy --token $FIREBASE_TOKEN
